{
  "type": "object",
  "properties": {
    "entities": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Human-readable name of the entity — a distinct, identifiable thing (person, organization, project, product, tool, place, concept, work, dataset, event, system)"
          },
          "entityType": {
            "type": "string",
            "description": "Category: person / organization / project / product / tool / place / concept / work / dataset / event / system"
          },
          "description": {
            "type": "string",
            "description": "1-2 sentence description of who/what this entity is"
          },
          "sourceContext": {
            "type": "string",
            "description": "Exact quote or context from the source that mentions this entity"
          }
        },
        "required": ["name", "entityType", "description", "sourceContext"],
        "additionalProperties": false
      }
    },
    "concepts": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Human-readable name of the concept (idea, theory, pattern, method, framework)"
          },
          "domain": {
            "type": "string",
            "description": "Domain or field the concept belongs to (e.g. 'philosophy/psychology')"
          },
          "description": {
            "type": "string",
            "description": "1-2 sentence description of the concept"
          },
          "sourceContext": {
            "type": "string",
            "description": "Exact quote or context from the source that discusses this concept"
          }
        },
        "required": ["name", "domain", "description", "sourceContext"],
        "additionalProperties": false
      }
    }
  },
  "required": ["entities", "concepts"],
  "additionalProperties": false
}
