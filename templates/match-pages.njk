You are matching extracted knowledge elements to existing wiki pages via semantic similarity.

Below is a JSON object containing two arrays:
- `unmatchedItems`: extracted {{pageType}} names that were NOT found by exact slug match. Each includes a description.
- `indexEntries`: summaries of all existing {{pageType}} pages from the wiki index.

Determine which existing pages (if any) semantically match the extracted names, even if the exact slug differs (e.g. "Cortisol reduction" ↔ "cortisol-senkung-durch-meditation").

```json
{
  "unmatchedItems": {{ unmatchedItems | dump(2) }},
  "indexEntries": {{ indexEntries | dump(2) }}
}
```

For each extracted name, decide: does it match any existing page semantically? Use the descriptions to disambiguate — two items with similar names but different descriptions should NOT be matched.

Only match when there is a strong semantic overlap (near-identical meaning or the same real-world referent). If no existing page describes substantially the same thing, mark it as unmatched. Do NOT force weak matches.

For every match and every unmatched name, provide a concise reason explaining your decision.
