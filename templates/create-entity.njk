You are creating a new entity page skeleton for the wiki. This is only a skeleton — claims, cross-connections, and open questions will be added later during the update phase. Entities are distinct, identifiable things — the "nouns" of the wiki: people, organizations, projects, products, tools, places, works, datasets, events, systems.

## The Entity You Are Creating

**Name:** {{ entityName }}
**Type:** {{ entityType }}
**Description:** {{ description }}
**Source context:** {{ sourceContext }}

## Source Page

This entity was extracted from: [[{{ sourcePath }}]]

### Source Content

{{ sourceContent }}

## Instructions

Return a JSON object with the following shape:

1. **title** — The canonical name of the entity.

2. **tldr** — A single sentence capturing the core point of this entity page.

3. **tags** — Array of lowercase tags based on the entity type and domain (e.g. ["philosophie", "stoizismus", "antike"]).

4. **body** — A concise encyclopedia-style description (2-3 paragraphs). Weave in the description and source context naturally. Write in the same language as the source content.

Do NOT generate claims, open questions, or cross-connections — those will be added in a later update step. Return only the JSON object with title, tldr, tags, and body.
